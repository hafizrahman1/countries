require 'spec_helper'

describe Countries do
  it 'has a version number' do
    expect(Countries::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
